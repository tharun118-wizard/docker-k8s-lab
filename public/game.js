const canvas = document.getElementById('gameCanvas'); const ctx = canvas.getContext('2d'); const scoreEl = document.getElementById('score'); const GRID = 20; let snake = [{x:10,y:10}], food = {x:15,y:15}; let dir = {x:1,y:0}, nextDir = {x:1,y:0}; let score = 0, running = false, iv; function draw() { ctx.fillStyle='#000'; ctx.fillRect(0,0,400,400); ctx.fillStyle='#4ade80'; snake.forEach(s=>ctx.fillRect(s.x*GRID,s.y*GRID,GRID-2,GRID-2)); ctx.fillStyle='#f87171'; ctx.fillRect(food.x*GRID,food.y*GRID,GRID-2,GRID-2); } function update() { dir = nextDir; const h = {x:snake[0].x+dir.x, y:snake[0].y+dir.y}; if(h.x<0||h.x>=20||h.y<0||h.y>=20||snake.some(s=>s.x===h.x&&s.y===h.y)){end();return;} snake.unshift(h); if(h.x===food.x&&h.y===food.y){ score+=10; scoreEl.textContent=score; food={x:Math.floor(Math.random()*20),y:Math.floor(Math.random()*20)}; } else { snake.pop(); } } function startGame(){ if(!running){running=true; iv=setInterval(()=>{update();draw();},200);} } function pauseGame(){ running=false; clearInterval(iv); } function end(){ pauseGame(); alert(`Game Over! Score: ${score}`); snake=[{x:10,y:10}]; score=0; scoreEl.textContent=0; draw(); } document.addEventListener('keydown',e=>{ const m={ArrowUp:{x:0,y:-1},ArrowDown:{x:0,y:1},ArrowLeft:{x:-1,y:0},ArrowRight:{x:1,y:0}}; if(m[e.key]) nextDir=m[e.key]; }); draw();
